# Copy to .env.test (gitignored) and fill in with the values `supabase start` prints locally.
# Used only by the integration-test fixture harness (tests/integration/support/*) — never read by app code.
SUPABASE_URL=###
SUPABASE_ANON_KEY=###
SUPABASE_SERVICE_ROLE_KEY=###
